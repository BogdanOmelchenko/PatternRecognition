language: python,cpp
python:
  - "3.6"      # current default Python on Travis CI
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  python -m doctest -v laba_3/laba_3.py
  cpp laba_4/ConsoleApplication1.cpp
